import React from 'react';
import HeaderNav from './HeaderNav';

function Header() {
    return (
        <header className="flex">
            <HeaderNav />
        </header>
    );
};

export default Header;